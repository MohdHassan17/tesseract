/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://www.tesseractdev.org", // replace with your domain
  generateRobotsTxt: true, // (optional) generates robots.txt too
  sitemapSize: 7000, // optional: splits large sitemaps
};
